import { describe, test } from '@jest/globals';
//import { GuildEmoji, GuildEmojiManager, Message } from "discord.js";

describe("EmojiCollection tests", () => {
  test('it finds an emoji by name', () => {

  });
});
